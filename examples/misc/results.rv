const safe_div = fn(a, b)
	if b == 0 {:err, :DivByZero}
	else {:ok, a / b}

print(safe_div(10, 2):unwrap()) # 5

match safe_div(10, 0)
	| {:ok, v}  => print(v)
	| {:err, e} => print(fmt("error: %v", e))

print({:ok, 42}[0] == :ok)    # :true
print({:err, :Boom}[0] == :err) # :true
